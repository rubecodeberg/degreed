{
  "name": "degreed-movie-mock",
  "version": "1.0.0",
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app -o"
  },
  "devDependencies": {
    "http-server": "^0.11.1"
  }
}
